/**
 * @fileoverview `/chat` 索引路由占位：`HomePageClient` 由 layout 持久挂载并依据 pathname 渲染。
 */

/**
 * 路由占位：`HomePageClient` 由 chat/layout 持久挂载，依据 pathname 渲染 /chat。
 */
export default function ChatIndexPage() {
  return null;
}
