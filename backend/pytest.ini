[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
markers =
    integration: 需要本机 PostgreSQL 等基础设施的接口测试
    searxng: 需要本机 SearXNG 容器；配合 SEARXNG_E2E=1 运行联网断言
    live_llm: 需要真实 LLM API Key 的 live 测试
